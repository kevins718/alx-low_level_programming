#include "main.h"

/**
  * mul - multiplies two integers
  * @a: The first number to multiply
  * @b: The second number to multiply with the first
  * Return: The result of multiplication between a parameter and b parameter
  */
int mul(int a, int b)
{
	return (a * b);
}
